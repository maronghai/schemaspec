# Generated by rune 0.333.0
# Do not edit manually.

type User {
  id: ID
  name: String!
}

type active_users {
  # Read-only view — columns derived from the SQL query
}

input UserInput {
  name: String!
}

type Query {
  """Fetch a users by ID."""
  user(id: ID!): User
  """List all users records."""
  usersList(limit: Int, offset: Int): [User!]!
  """Fetch from view active_users."""
  active_users: [active_users!]!
}

type Mutation {
  """Create a new users."""
  createUser(input: UserInput!): User!
  """Update an existing users."""
  updateUser(id: ID!, input: UserInput!): User!
  """Delete a users."""
  deleteUser(id: ID!): Boolean!
}
