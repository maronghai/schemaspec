{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "rune-schema",
  "type": "object",
  "$defs": {
    "t": {
      "type": "object",
      "description": "用户表",
      "properties": {
        "id": {"type":"integer"},
        "name": {"type":"string"},
        "email": {"type":"string"},
        "status": {"type":"boolean"}
      },
      "required": ["id", "name", "email", "status"],
      "additionalProperties": false
    }
  },
  "properties": {
    "t": { "$ref": "#/$defs/t" }
  },
  "required": ["t"]
}
