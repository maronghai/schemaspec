model users {
  id Int @id @default(autoincrement())
  org Int
  name String

  @@map("users")
}

model memberships {
  user_id Int
  org_id Int
  user users? @relation("memberships_users", fields: [user_id, org_id], references: [id, org])

  @@map("memberships")
}
